# set keyboard
setxkbmap -variant altgr-intl -option ctrl:nocaps

# fix dpi
xrandr --dpi 192

# additional keybindings
xbindkeys

# touchpad gestures
libinput-gestures &

# start ssh-agent
eval $(ssh-agent)
